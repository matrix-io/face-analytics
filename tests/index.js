var should = require('chai').should(),
    expect = require('chai').expect()
    face = require('../index');

describe("Face", function() {
  it.skip("API Key > Success", function() {
  });

  it.skip("Detect > Fail, No Image", function() {
  });

  it.skip("Detect > Fail, No API Key", function() {
  });

  it.skip("Detect > Success", function() {
  });
});